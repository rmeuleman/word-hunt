import { WordLengthValidatorDirective } from './word-length.directive';

describe('WordLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new WordLengthValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
